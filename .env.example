# GitHub Token für private Repositories (ERFORDERLICH wenn Repos privat sind!)
# Erstelle ein Token unter: https://github.com/settings/tokens/new?scopes=repo
GITHUB_TOKEN=ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Access Token für Satis Repository (generiere einen sicheren zufälligen String)
# Verwende z.B.: openssl rand -hex 32
# Wenn leer, ist das Repository ÖFFENTLICH zugänglich!
ACCESS_TOKEN=your-secure-random-token-here

# Build-Intervall in Sekunden (default: 900 = 15 Minuten)
SATIS_BUILD_INTERVAL=900

# Satis Konfiguration
SATIS_NAME=your-vendor/packages
SATIS_HOMEPAGE=https://packages.your-domain.com
SATIS_DESCRIPTION=Private Composer Package Repository

# Satis Repositories (JSON Array Format)
# Beispiel für zwei Repositories:
SATIS_REPOSITORIES=[{"type":"vcs","url":"https://github.com/YOUR-USERNAME/package-one.git"},{"type":"vcs","url":"https://github.com/YOUR-USERNAME/package-two.git"}]
